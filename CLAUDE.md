# CLAUDE.md — AI 协作纪律协议

> 本文件是项目协议层（L1）的**单一真相源**。
> 任何接入本仓库的 AI 助手（Claude Code / Cursor / 其他）必须严格遵守。
> 用户也以本文件为准。

---

## 🔒 会话启动协议（每次必读，不可跳过）

每次新会话必须按顺序执行以下步骤，**不得跳过任何一步**：

1. Read `CLAUDE.md`（本文件）
2. Read `docs/session_logs/` 下最新 1 个文件（查看上次会话状态）
3. Read `docs/lessons.md`（避免重犯已知陷阱）
4. Read `docs/specs/README.md`（获取业务规则索引）
5. **根据用户任务**，按需 Read 相关的 `docs/specs/*.md`（只读相关的，不要全读）
6. Read 任务涉及模块的现有代码（Grep/Read 实际代码，**不凭记忆**）
7. 用中文向用户**复述理解**：
   - 当前项目状态（来自 session log）
   - 我理解的任务目标
   - 涉及的业务规则（引用 specs 文件名）
   - 可能的风险点
8. 等待用户确认后再动手编码

---

## 🔒 改代码纪律（反幻觉）

### 改代码前必做

- [ ] 涉及新字段/新表 → 先用 inspector 确认 DB 真实 schema（**不要假设列已存在**）
- [ ] 涉及新函数调用 → 先 Grep 确认目标存在
- [ ] 涉及 API 响应 → Read 后端响应格式，别假设字段名
- [ ] HTTP client 是否已经自动解包 `response.data`（不同封装行为不一样）

### 改代码后必做

- [ ] Python 改动 → `python -m py_compile` 验证
- [ ] 前端改动 → 跑 build（不是只看类型检查）
- [ ] DB schema 改动 → 同时给出 **rollback SQL**

### 高风险操作必做

高风险操作定义：删除数据 / 改 DB schema / 改权限 / 改审计中间件 / 改电子签名 / 改鉴权流程

这些操作**必须**：

- [ ] 先给用户看**影响清单**（哪些模块会受影响）
- [ ] 先给用户看**回滚方案**（出问题怎么恢复）
- [ ] 等用户确认后再执行

### 输出必须三层分明

- ✅ **已验证** — 附工具输出为证据（grep 结果、build 输出、DB 查询结果）
- 🔶 **高置信推断** — 附推理链（基于 X 和 Y，推断 Z）
- ❓ **需确认** — 附具体问题（某字段含义、某业务规则分支）

### 禁止事项

- 禁止说"应该能工作"、"通常这样做"
- 禁止凭印象引用函数/字段名
- 禁止编造测试结果
- 禁止在未读 `docs/specs/` 的情况下处理业务逻辑
- 禁止违反下方 5 条绝对禁令

---

## 📏 文件规模红线

**背景**：AI 辅助开发的典型失败模式 —— 每次加功能都往最近的文件堆，N 次会话累计成单文件 3000 行。这种巨型文件已经成为不可维护技术债。

### 硬上限

| 文件类型 | 警戒线 | 硬上限 | 超限行为 |
|----------|--------|--------|----------|
| 前端页面（`.jsx` / `.tsx`） | **400 行** | **600 行** | 超 600 行**禁止再加新功能**，必须先拆 |
| 前端通用组件 | **300 行** | **500 行** | 同上 |
| 后端路由文件（`.py`） | **500 行** | **800 行** | 同上（除 generated 类） |
| 后端 service 文件 | **600 行** | **1000 行** | 算法文件可例外但要注释说明 |
| 数据库迁移 | **400 行** | **600 行** | 多次操作可拆多个 migration |

### 修改前强制检查

**改任何文件前必须先**：

```bash
wc -l <目标文件>
```

- 若 < 警戒线：正常改
- 若 ≥ 警戒线 且 < 硬上限：**警告用户**，问是否顺手拆一部分
- 若 ≥ 硬上限：**拒绝直接加新功能**，先产出拆分方案给用户

### 新建文件前强制检查

- 估算新文件最终大小；如预计 > 警戒线，**先设计模块拆分**再写
- 例：新页面前先想"Tab 分 3 个子组件 + 共享 hook"，而不是一个 `.jsx` 全写

### 修改已超标文件的强制条款

**修改任何已超过硬上限的文件时，同次修改必须**：

1. 至少拆出 1 个新子组件 / 新模块
2. 新子组件放独立文件（遵循自身规模限制）
3. 原文件行数**必须减少**（不可净增加）

---

## 🚫 操作纪律 · 5 条绝对禁令

**背景**：过去会话里出现过"擅自批量替换"、"顺手删除功能"、"简化用户要求"、"拆分引入新 bug"等问题。以下 5 条**绝对**禁令，任何时候都不能违反。

### 禁令 1：批量替换禁止跨多文件一把梭

**禁止**：

- ❌ 用 `sed -i` / `grep -rl ... | xargs` 批量替换多文件
- ❌ 一次 Edit 多个文件的同类硬编码
- ❌ "顺手"改其他文件的字符串

**必须**：

- ✅ **单文件进行** — 一次只处理一个文件
- ✅ **完成后立即验证**（grep 残留 + build + 浏览器肉眼）
- ✅ 过完一个文件再动下一个
- ✅ 每个文件改完先 commit，有问题能精确回退

### 禁令 2：拆分大文件必须单个进行 + 立即测试

**禁止**：

- ❌ 一次拆多个文件
- ❌ "先全部拆完再一起测"
- ❌ 拆完直接 commit 没跑验证

**必须**：

- ✅ **一次只拆一个文件**
- ✅ **拆完立刻测**（build / py_compile / 主路径手测）
- ✅ `wc -l` 验证原文件行数确实减少
- ✅ 测试通过后再动下一个文件
- ✅ 每次拆分单独 commit（"refactor: split X into Y, Z"）

### 禁令 3：禁止删除功能

**禁止**：

- ❌ 拆分/重构过程中"顺手"删除看起来冗余的代码
- ❌ 注释掉功能后认为"可以删了"
- ❌ 即使认为某 function 废弃，也不能自行删
- ❌ 删注释 / 删 TODO / 删 docstring

**必须**：

- ✅ **保留所有原有功能**，除非用户明确同意删除
- ✅ 觉得某段代码废弃 → **先问用户**，等确认
- ✅ 不确定时 **保留 + 标 `# DEPRECATED: ...`** 比直接删安全
- ✅ 拆分时函数只能**移动**（从 A 文件到 B 文件），不能**消失**

### 禁令 4：禁止对用户指令做任何降级处理

**禁止**：

- ❌ "用户说全做我做一半，方便点"
- ❌ "用户说完整版，但 context 紧我改成精简版"
- ❌ "用户要装饰器，但我觉得 inline 更简单就 inline 了"
- ❌ "用户列 20 条我挑 5 条做"
- ❌ 任何形式的"偷工减料"

**必须**：

- ✅ 用户要求的**规格、数量、详细度、覆盖面**一字不改执行
- ✅ 真的做不到（context / 时间 / 能力限制）→ **先请示**，说清楚限制 + 备选方案
- ✅ 等用户**明确同意**降级后再降级
- ✅ 被迫降级时在代码 / 文档里**明确标注**：
  ```
  # TODO: 原要求 X，当前降级为 Y（原因：Z），待补齐
  ```

### 禁令 5：禁止引入新的隐患

**禁止**：

- ❌ 修 A 问题引入 B 问题（改后比改前更脆弱）
- ❌ 拆分引入新 bug（功能看似在但边缘情况坏了）
- ❌ 抄了网上的"最佳实践"但没结合本项目验证
- ❌ 改完没跑 build / test 就提交
- ❌ 引入新依赖（新库、新字段、新表）没先告诉用户

**必须**：

- ✅ **改完必做回归**：至少覆盖被改代码的主路径 + 1 个边缘场景
- ✅ **改完前后对比**：`git diff` 自查，问自己"这个改动会破坏什么"
- ✅ 引入新依赖前**先请示**（新库、新字段、新表、新环境变量）
- ✅ 改完在对话里**明确报告**：改了什么 / 验证了什么 / 有哪些没验证（老实说）

---

## 📋 改完必须给用户的报告格式

```
✅ 已改：<文件>:<行号>
✅ 已验证：<具体命令和输出>
⚠️ 未验证：<哪些场景我没测，请你留意>
🔶 潜在影响：<改动可能影响的其他模块>
```

**禁止**只说"改完了"不给证据。

---

## 📊 会话结束协议

每次重大会话结束时，AI 必须主动：

1. 生成 / 更新 `docs/session_logs/YYYY-MM-DD.md`（今日改动、关键决策、陷阱、待办）
2. 如有新踩的坑，更新 `docs/lessons.md`
3. 如有新的业务规则被用户确认，更新对应 `docs/specs/*.md`
4. 如需后续部署，明确列出：代码 commit hash + DB SQL + 验证步骤

---

## 🛠 技术栈占位（按你项目实际填写）

> 这里只是占位骨架。Fork 这个 demo 后请把这一节替换成你自己项目的真实技术栈。

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | （例：React + TypeScript） | （端口 / 构建器） |
| 后端 | （例：Python FastAPI） | （ASGI / 端口） |
| 数据库 | （例：PostgreSQL 16） | （连接池配置） |
| 缓存 | （例：Redis 7） | （用途：黑名单 / 会话 / 限流） |
| 认证 | （例：JWT + RBAC） | （角色数 / token 机制） |
| 测试 | （例：pytest + Jest） | （合约测试 / 单元 / 端到端） |

---

## 📁 目录结构占位

> 同上，Fork 后请按你的真实结构填写。

```
your-project/
├── backend/             # 后端代码
├── frontend/            # 前端代码
├── docs/                # 知识层（参见 docs/README.md）
├── scripts/ci/          # CI 门禁脚本
├── migrations/          # 数据库迁移
└── tests/               # 合约测试 + 单元测试
```

---

## 🔧 代码规范要点（占位）

### 后端

- 路由按模块分文件，集中注册
- 服务分层：写操作 / 读操作 / 实体协调器分开
- Schema 变更走迁移工具，不手写 ALTER
- 配置统一从 `settings.py` 读，不直接读环境变量

### 前端

- API 调用统一用 client 封装，不用裸 fetch
- 组件 / 页面 / 路由分目录
- 操作按钮需要 tooltip 文字说明
- React Hook 必须放在所有 conditional return 之前（见 `docs/lessons.md`）

### 数据库

- 通过迁移工具管理，不手 ALTER
- 备份脚本固定路径
- 保留 SQLite 兼容代码（如果有）但生产用真实数据库

---

## 👤 开发者背景占位

> Fork 后请按你的真实情况填写。这一节让 AI 理解你的能力边界，给出更合适的协作方式。

- **开发者背景**：（例：生物 R&D + AI 辅助开发，无正式编程背景）
- **AI 协作偏好**：改代码时请给出**完整的文件内容或明确的 diff**，不要只说"修改第 X 行"
- **沟通语言**：默认中文

---

## 🎯 Design Context 占位

> 视情况填写，对设计 / UI 类任务有帮助。

### Users
- **Primary**：你的目标用户群
- **Context**：使用场景、设备、屏幕尺寸
- **Job to be done**：用户来这里要完成什么
- **Emotional goal**：用户使用时的情感目标（高效 / 可控 / 信赖）

### Brand Personality
- **3-word personality**：用三个词描述品牌调性
- **References**：（参考网站 / 风格参考）
- **Anti-references**：（你不想要的样子）

### Design Principles
1. （例：Density with Clarity — 一屏展示尽可能多的信息，通过排版层次保持可读性）
2. （例：Subtle Feedback — 微妙但确定的交互反馈，1-2px translateY 而非 8px 弹跳）
3. （例：GMP-Grade Precision — 数据精确对齐，状态颜色 + 文字双编码）

---

> **本文件是协议，不是建议。AI 在本仓库工作时必须严格执行。**
